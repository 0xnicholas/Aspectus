# v0.1.0 — 项目骨架 技术规格

> 版本：v0.1.0
> 状态：已冻结
> 关联：ROADMAP.md v0.1.0
> 实现计划：[../plans/v0.1.0-implementation.md](../plans/v0.1.0-implementation.md)

---

## 1. 目标

交付一个**可编译、可运行、可测试**的 Rust workspace，包含完整的数据库 schema 和开发基础设施。不实现任何业务逻辑。

---

## 2. 仓库结构

```
Aspectus/
├── Cargo.toml                  # workspace root
├── Cargo.lock
├── ROADMAP.md
├── AGENTS.md
├── README.md
├── .gitignore
├── .env.example
├── justfile
├── docker-compose.yml
├── .github/
│   └── workflows/
│       └── ci.yml
├── crates/
│   ├── aspectus-core/
│   │   ├── Cargo.toml
│   │   └── src/
│   │       ├── lib.rs
│   │       ├── tenant.rs
│   │       ├── service_account.rs
│   │       ├── api_key.rs
│   │       ├── scope.rs
│   │       ├── user.rs
│   │       ├── role.rs
│   │       ├── audit_log.rs
│   │       ├── project.rs
│   │       ├── identity.rs
│   │       └── error.rs
│   ├── aspectus-auth/
│   │   ├── Cargo.toml
│   │   └── src/
│   │       └── lib.rs              # stub：trait 声明，v0.2.0 实现
│   ├── aspectus-server/
│   │   ├── Cargo.toml
│   │   └── src/
│   │       ├── main.rs             # axum 启动 + /health
│   │       ├── config.rs           # 环境变量加载
│   │       ├── error.rs            # RFC 7807 error response
│   │       └── db/
│   │           └── mod.rs          # sqlx 连接池初始化
│   └── aspectus-client/
│       ├── Cargo.toml
│       └── src/
│           └── lib.rs              # stub：client struct 声明，v0.2.0 实现
├── migrations/
│   ├── 20260531000001_initial_schema.sql
│   └── 20260531000002_indexes_and_triggers.sql
├── tests/
│   └── integration/
│       └── health.rs               # testcontainers 集成测试
└── docs/
    ├── adr/                        # 15 篇 ADR
    └── superpowers/
```

---

## 3. Cargo Workspace

### 3.1 Root `Cargo.toml`

```toml
[workspace]
resolver = "2"
members = [
    "crates/aspectus-core",
    "crates/aspectus-auth",
    "crates/aspectus-server",
    "crates/aspectus-client",
]

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/pandaria/aspectus"

[workspace.dependencies]
# Async runtime
tokio = { version = "1", features = ["full"] }

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Database
sqlx = { version = "0.8", features = ["runtime-tokio", "tls-rustls", "postgres", "chrono"] }

# HTTP
axum = "0.8"
tower = "0.5"
tower-http = { version = "0.6", features = ["cors", "trace"] }
hyper = "1"

# Crypto
sha2 = "0.10"
argon2 = "0.5"

# IDs
ksuid = "1"

# Caching
redis = { version = "0.28", features = ["tokio-comp", "connection-manager"] }

# Observability
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }
tracing-appender = "0.2"

# Error handling
anyhow = "1"
thiserror = "2"
mime = "0.3"

# Testing
testcontainers = "0.23"
testcontainers-modules = { version = "0.11", features = ["postgres", "redis"] }

# Time
chrono = { version = "0.4", features = ["serde"] }

# Config
dotenvy = "0.15"

# Logging (for sqlx)
log = "0.4"
```

### 3.2 `crates/aspectus-core/Cargo.toml`

```toml
[package]
name = "aspectus-core"
version.workspace = true
edition.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
chrono.workspace = true
ksuid.workspace = true
sqlx.workspace = true       # for FromRow derive + postgres types
sha2.workspace = true       # for ApiKey hash type
```

**依赖原则**：`aspectus-core` 只依赖序列化和数据库类型映射。不依赖 HTTP、不依赖 Redis、不依赖 axum。

### 3.3 `crates/aspectus-auth/Cargo.toml`

```toml
[package]
name = "aspectus-auth"
version.workspace = true
edition.workspace = true

[dependencies]
aspectus-core = { path = "../aspectus-core" }
sha2.workspace = true
argon2.workspace = true
redis.workspace = true
sqlx.workspace = true
thiserror.workspace = true
tracing.workspace = true
```

**v0.1.0 状态**：仅声明 trait 和空 struct，不实现验证逻辑。不需要 Redis 连接（但依赖已声明供 v0.2.0）。

### 3.4 `crates/aspectus-server/Cargo.toml`

```toml
[package]
name = "aspectus-server"
version.workspace = true
edition.workspace = true

[dependencies]
aspectus-core = { path = "../aspectus-core" }
aspectus-auth = { path = "../aspectus-auth" }
axum.workspace = true
tower.workspace = true
tower-http.workspace = true
hyper.workspace = true
tokio.workspace = true
serde.workspace = true
serde_json.workspace = true
sqlx.workspace = true
redis.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true
dotenvy.workspace = true
chrono.workspace = true
mime.workspace = true
thiserror.workspace = true
```

### 3.5 `crates/aspectus-client/Cargo.toml`

```toml
[package]
name = "aspectus-client"
version.workspace = true
edition.workspace = true

[dependencies]
aspectus-core = { path = "../aspectus-core" }
serde.workspace = true
serde_json.workspace = true
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
thiserror.workspace = true
```

**v0.1.0 状态**：仅声明 `AspectusClient` struct 和 `IntrospectRequest`/`IntrospectResponse` 类型，不实现 HTTP 调用。

---

## 4. 数据库 Schema

> **策略**：v0.1.0 一次性建好所有四个版本需要的全部表。后续版本只做增量 migration（INSERT 种子数据、ADD COLUMN、ADD CONSTRAINT）。避免生产环境有数据后再做大表 DDL。

### 4.1 Migration 001 — 初始表结构

```sql
-- ============================================================
-- Migration: 20260531000001_initial_schema.sql
-- 描述：创建所有版本需要的全部表。v0.2.0-v1.0.0 各版本逐步激活使用。
-- ============================================================

-- -----------------------------------------------------------
-- 枚举类型
-- -----------------------------------------------------------

-- 身份类型：区分人类用户和机器账号 (ADR-004)
create type identity_type as enum ('user', 'service_account');

-- 项目枚举：生态中的所有系统 (ADR-010)
create type project as enum (
    'pandaria',
    'tavern',
    'emerald',
    'constell',
    'tokencamp',
    'heirloom'
);

-- 角色类型：约束 Role 可被赋予哪种身份 (ADR-004, ADR-005)
-- 参考 Logto 的 role_type 设计，扩展了 'both'
create type role_type as enum ('user', 'service_account', 'both');

-- 密码加密方法枚举 (参考 Logto)
-- v1.0.0 使用 argon2id，保留其他值为未来扩展
create type password_encryption_method as enum (
    'Argon2id'
);

-- -----------------------------------------------------------
-- v0.2.0 激活：核心业务表
-- -----------------------------------------------------------

-- 租户 (ADR-008: 单层多租户)
create table tenants (
    id          varchar(21) primary key,               -- KSUID, e.g. "org_aBcDeFgHiJkLmNoPqRsT"
    name        varchar(128) not null,
    quotas      jsonb not null default '{}',            -- per-project 配额配置 (v0.3.0 激活)
    created_at  timestamptz not null default now()
);

-- 服务账号 (ADR-004)
create table service_accounts (
    id          varchar(21) primary key,
    tenant_id   varchar(21) not null references tenants(id)
                    on update cascade on delete cascade,
    label       varchar(128) not null,                  -- 显示名称
    description text,
    expires_at  timestamptz,                            -- 账号级过期，null = 永不过期
    created_at  timestamptz not null default now()
);

create index service_accounts__tenant
    on service_accounts (tenant_id, id);

-- API Key (ADR-002)
create table api_keys (
    id              varchar(21) primary key,
    tenant_id       varchar(21) not null references tenants(id)
                        on update cascade on delete cascade,
    service_account_id varchar(21) not null references service_accounts(id)
                        on update cascade on delete cascade,
    project         project not null,                   -- 绑定单一生态项目
    key_hash        varchar(64) not null,               -- sha256(key)
    key_prefix      varchar(32) not null,               -- "pk_live_" + KSUID前缀
    scopes          text[] not null default '{}',       -- scope 数组，v0.3.0 做校验
    expires_at      timestamptz,                        -- Key 级过期，null = 永不过期
    revoked_at      timestamptz,                        -- null = 有效，非 null = 已吊销
    created_at      timestamptz not null default now()
);

-- key_hash 唯一索引兼查找索引：覆盖 sha256(token) 等值查找
create unique index api_keys__key_hash
    on api_keys (key_hash);

create index api_keys__service_account
    on api_keys (tenant_id, service_account_id, project);

-- FK lookup 索引：CASCADE delete 时高效查找引用行
create index api_keys__service_account_id
    on api_keys (service_account_id);

-- 审计日志 (ADR-009)
create table audit_logs (
    id          varchar(21) primary key,
    tenant_id   varchar(21) not null references tenants(id)
                    on update cascade on delete cascade,
    actor_id    varchar(21) not null,                   -- User 或 Service Account 的 ID
    actor_type  identity_type not null,
    action      varchar(64) not null,                   -- e.g. 'api_key.created'
    target_type varchar(32) not null,                   -- e.g. 'api_key'
    target_id   varchar(21) not null,
    metadata    jsonb not null default '{}',            -- 额外上下文，绝不包含密钥
    created_at  timestamptz not null default now()
);

create index audit_logs__tenant
    on audit_logs (tenant_id, created_at desc);

create index audit_logs__actor
    on audit_logs (tenant_id, actor_id, created_at desc);

create index audit_logs__action
    on audit_logs (tenant_id, action, created_at desc);

-- Service Token (ADR-011)
create table service_tokens (
    project     project not null primary key,           -- 每个 Project 恰好一个
    token_hash  varchar(64) not null,                   -- sha256(service_token)
    created_at  timestamptz not null default now(),
    updated_at  timestamptz not null default now()
);

-- -----------------------------------------------------------
-- v0.3.0 激活：Scope 定义
-- -----------------------------------------------------------

-- Scope 定义 (ADR-006)
-- v0.3.0 写入种子数据，v0.2.0 此表为空
create table scopes (
    id      varchar(21) primary key,
    name    varchar(256) not null,                      -- e.g. "pandaria:session:create"
    description text,
    constraint scopes__name unique (name)
);

-- -----------------------------------------------------------
-- v1.0.0 激活：用户和角色
-- -----------------------------------------------------------

-- 用户 (ADR-004)
-- v1.0.0 开始使用，v0.1.0 建好避免后续 DDL 锁
create table users (
    id                          varchar(21) primary key,
    tenant_id                   varchar(21) not null references tenants(id)
                                    on update cascade on delete cascade,
    email                       varchar(256),
    password_hash               varchar(256),           -- argon2id hash
    password_encryption_method  password_encryption_method default 'Argon2id',
    display_name                varchar(128),
    is_suspended                boolean not null default false,
    last_sign_in_at             timestamptz,
    created_at                  timestamptz not null default now(),
    updated_at                  timestamptz not null default now(),
    constraint users__email unique (tenant_id, email)
);

create index users__tenant
    on users (tenant_id, id);

-- 角色 (ADR-005)
create table roles (
    id          varchar(21) primary key,
    name        varchar(128) not null,
    description varchar(256),
    type        role_type not null default 'user',      -- 约束可被赋予的身份类型
    is_default  boolean not null default false,          -- 是否新用户默认角色
    constraint roles__name unique (name)
);

create index roles__type
    on roles (type);

-- 角色-权限关联 (ADR-005)
create table roles_scopes (
    id          varchar(21) primary key,
    role_id     varchar(21) not null references roles(id)
                    on update cascade on delete cascade,
    scope_id    varchar(21) not null references scopes(id)
                    on update cascade on delete cascade,
    constraint roles_scopes__unique unique (role_id, scope_id)
);

-- 用户-角色关联 (ADR-004, ADR-005)
create table users_roles (
    id          varchar(21) primary key,
    user_id     varchar(21) not null references users(id)
                    on update cascade on delete cascade,
    role_id     varchar(21) not null references roles(id)
                    on update cascade on delete cascade,
    constraint users_roles__unique unique (user_id, role_id)
);

-- role_type 约束函数：确保赋予用户的 Role 类型匹配 (参考 Logto)
create function check_role_type(
    target_role_id  varchar(21),
    allowed_types   role_type[]
) returns boolean as $$
begin
    return (select type from roles where id = target_role_id) = any(allowed_types);
end;
$$ language plpgsql;

-- v1.0.0 激活时添加 users_roles 的 check constraint
-- alter table users_roles
--     add constraint users_roles__role_type
--     check (check_role_type(role_id, array['user', 'both']::role_type[]));
-- 注意：此约束在 v1.0.0 migration 中激活，不在 v0.1.0 激活
--       因为 v0.1.0 时 roles 表为空，check 函数调用无意义
```

### 4.2 Migration 002 — 索引与触发器

```sql
-- ============================================================
-- Migration: 20260531000002_indexes_and_triggers.sql
-- 描述：补充分散在 001 之外的索引和自动更新触发器
-- ============================================================

-- api_keys 按 tenant 查询（管理 API：列出 tenant 的所有 Key）
-- 注意：api_keys__key_hash、api_keys__service_account、
--       api_keys__service_account_id 已在 001 中创建
create index api_keys__tenant
    on api_keys (tenant_id, created_at desc);

-- audit_logs 按 target 查询
create index audit_logs__target
    on audit_logs (target_type, target_id);

-- service_tokens 按 token_hash 查找（/introspect 认证热路径）
create index service_tokens__token_hash
    on service_tokens (token_hash);

-- 触发器：自动更新 updated_at
create or replace function set_updated_at()
returns trigger as $$
begin
    new.updated_at = now();
    return new;
end;
$$ language plpgsql;

create trigger set_users_updated_at
    before update on users
    for each row execute procedure set_updated_at();

create trigger set_service_tokens_updated_at
    before update on service_tokens
    for each row execute procedure set_updated_at();
```

---

## 5. 域模型 (`aspectus-core`)

> v0.1.0 只定义 struct 和 trait 签名。所有方法体为空（`todo!()`）或返回 `unimplemented!()`。

### 5.1 Project 枚举

```rust
// crates/aspectus-core/src/project.rs

use serde::{Deserialize, Serialize};
use sqlx::Type;

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize, Type)]
#[sqlx(type_name = "project", rename_all = "lowercase")]
pub enum Project {
    Pandaria,
    Tavern,
    Emerald,
    Constell,
    Tokencamp,
    Heirloom,
}

impl std::fmt::Display for Project {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Pandaria => write!(f, "pandaria"),
            Self::Tavern => write!(f, "tavern"),
            Self::Emerald => write!(f, "emerald"),
            Self::Constell => write!(f, "constell"),
            Self::Tokencamp => write!(f, "tokencamp"),
            Self::Heirloom => write!(f, "heirloom"),
        }
    }
}

impl std::str::FromStr for Project {
    type Err = crate::error::CoreError;
    // ...
}
```

### 5.2 Identity 枚举

```rust
// crates/aspectus-core/src/identity.rs

use serde::{Deserialize, Serialize};
use sqlx::Type;

#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize, Type)]
#[sqlx(type_name = "identity_type", rename_all = "lowercase")]
pub enum IdentityType {
    #[serde(rename = "user")]
    User,
    #[serde(rename = "service_account")]
    #[sqlx(rename = "service_account")]
    ServiceAccount,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize, Type)]
#[sqlx(type_name = "role_type", rename_all = "lowercase")]
pub enum RoleType {
    User,
    #[sqlx(rename = "service_account")]
    ServiceAccount,
    Both,
}
```

### 5.3 核心 struct

```rust
// crates/aspectus-core/src/tenant.rs
use chrono::{DateTime, Utc};
use serde::{Deserialize, Serialize};
use sqlx::FromRow;

#[derive(Debug, Clone, Serialize, Deserialize, FromRow)]
pub struct Tenant {
    pub id: String,
    pub name: String,
    pub quotas: serde_json::Value,  // JSONB → serde_json::Value
    pub created_at: DateTime<Utc>,
}
```

```rust
// crates/aspectus-core/src/service_account.rs
#[derive(Debug, Clone, Serialize, Deserialize, FromRow)]
pub struct ServiceAccount {
    pub id: String,
    pub tenant_id: String,
    pub label: String,
    pub description: Option<String>,
    pub expires_at: Option<DateTime<Utc>>,
    pub created_at: DateTime<Utc>,
}
```

```rust
// crates/aspectus-core/src/api_key.rs
use crate::project::Project;

#[derive(Debug, Clone, Serialize, Deserialize, FromRow)]
pub struct ApiKey {
    pub id: String,
    pub tenant_id: String,
    pub service_account_id: String,
    pub project: Project,
    pub key_hash: String,
    pub key_prefix: String,
    pub scopes: Vec<String>,
    pub expires_at: Option<DateTime<Utc>>,
    pub revoked_at: Option<DateTime<Utc>>,
    pub created_at: DateTime<Utc>,
}

/// API Key 创建后返回给调用方的结构（含原文，仅此一次）
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreatedApiKey {
    pub id: String,
    pub key: String,            // 原文 "pk_live_..."
    pub key_prefix: String,
    pub project: Project,
    pub scopes: Vec<String>,
    pub expires_at: Option<DateTime<Utc>>,
}

/// API Key 列表项（不含 hash 和原文）
#[derive(Debug, Clone, Serialize, Deserialize, FromRow)]
pub struct ApiKeyListItem {
    pub id: String,
    pub service_account_id: String,
    pub project: Project,
    pub key_prefix: String,
    pub scopes: Vec<String>,
    pub expires_at: Option<DateTime<Utc>>,
    pub revoked_at: Option<DateTime<Utc>>,
    pub created_at: DateTime<Utc>,
}
```

```rust
// crates/aspectus-core/src/scope.rs

#[derive(Debug, Clone, Serialize, Deserialize, FromRow)]
pub struct Scope {
    pub id: String,
    pub name: String,
    pub description: Option<String>,
}
```

```rust
// crates/aspectus-core/src/audit_log.rs
use crate::identity::IdentityType;

#[derive(Debug, Clone, Serialize, Deserialize, FromRow)]
pub struct AuditLog {
    pub id: String,
    pub tenant_id: String,
    pub actor_id: String,
    pub actor_type: IdentityType,
    pub action: String,
    pub target_type: String,
    pub target_id: String,
    pub metadata: serde_json::Value,
    pub created_at: DateTime<Utc>,
}
```

```rust
// crates/aspectus-core/src/user.rs (v1.0.0 激活)

#[derive(Debug, Clone, Serialize, Deserialize, FromRow)]
pub struct User {
    pub id: String,
    pub tenant_id: String,
    pub email: Option<String>,
    // password_hash: String,  // 不实现 Serialize！绝不暴露到外部
    #[serde(skip)]
    pub password_hash: Option<String>,
    pub display_name: Option<String>,
    pub is_suspended: bool,
    pub last_sign_in_at: Option<DateTime<Utc>>,
    pub created_at: DateTime<Utc>,
    pub updated_at: DateTime<Utc>,
}
```

```rust
// crates/aspectus-core/src/role.rs
use crate::identity::RoleType;

#[derive(Debug, Clone, Serialize, Deserialize, FromRow)]
pub struct Role {
    pub id: String,
    pub name: String,
    pub description: Option<String>,
    pub r#type: RoleType,
    pub is_default: bool,
}
```

### 5.4 `/introspect` 响应 struct

```rust
// crates/aspectus-core/src/introspect.rs
use crate::identity::IdentityType;
use std::collections::HashMap;

/// RFC 7662 introspection 响应 (ADR-001)
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct IntrospectResponse {
    pub active: bool,

    // 以下字段仅在 active = true 时出现
    #[serde(skip_serializing_if = "Option::is_none")]
    pub tenant_id: Option<String>,

    #[serde(skip_serializing_if = "Option::is_none")]
    pub user_id: Option<String>,

    #[serde(skip_serializing_if = "Option::is_none")]
    pub identity_type: Option<IdentityType>,

    #[serde(skip_serializing_if = "Option::is_none")]
    pub client_id: Option<String>,

    #[serde(skip_serializing_if = "Option::is_none")]
    pub scope: Option<String>,

    #[serde(skip_serializing_if = "Option::is_none")]
    pub token_type: Option<String>,

    #[serde(skip_serializing_if = "Option::is_none")]
    pub exp: Option<i64>,

    // v0.3.0 新增
    #[serde(skip_serializing_if = "Option::is_none")]
    pub quotas: Option<HashMap<String, serde_json::Value>>,
}

impl IntrospectResponse {
    pub fn inactive() -> Self {
        Self {
            active: false,
            tenant_id: None,
            user_id: None,
            identity_type: None,
            client_id: None,
            scope: None,
            token_type: None,
            exp: None,
            quotas: None,
        }
    }
}
```

### 5.5 错误类型

```rust
// crates/aspectus-core/src/error.rs
use thiserror::Error;

#[derive(Debug, Error)]
pub enum CoreError {
    #[error("invalid project: {0}")]
    InvalidProject(String),

    #[error("invalid scope format: {0}")]
    InvalidScope(String),

    #[error("entity not found: {entity} id={id}")]
    NotFound { entity: &'static str, id: String },

    #[error("validation failed: {0}")]
    Validation(String),

    #[error("internal error: {0}")]
    Internal(String),
}
```

### 5.6 lib.rs 入口

```rust
// crates/aspectus-core/src/lib.rs

pub mod tenant;
pub mod service_account;
pub mod api_key;
pub mod scope;
pub mod user;
pub mod role;
pub mod audit_log;
pub mod project;
pub mod identity;
pub mod introspect;
pub mod error;
```

---

## 6. HTTP 服务 (`aspectus-server`)

### 6.1 启动入口

```rust
// crates/aspectus-server/src/main.rs

use axum::{Router, routing::get};
use std::net::SocketAddr;
use tower_http::cors::CorsLayer;
use tower_http::trace::TraceLayer;
use tracing_subscriber::{layer::SubscriberExt, util::SubscriberInitExt};

mod config;
mod error;
mod db;

#[tokio::main]
async fn main() -> anyhow::Result<()> {
    // 1. 加载 .env（开发环境）
    let _ = dotenvy::dotenv();

    // 2. 初始化 tracing
    tracing_subscriber::registry()
        .with(tracing_subscriber::EnvFilter::try_from_default_env()
            .unwrap_or_else(|_| "aspectus_server=debug,tower_http=debug".into()))
        .with(tracing_subscriber::fmt::layer())
        .init();

    // 3. 加载配置
    let config = config::Config::from_env()?;

    // 4. 初始化数据库连接池
    let _db_pool = db::init_pool(&config.database_url).await?;

    // 5. 构建路由
    let app = Router::new()
        .route("/health", get(|| async { "ok" }))
        .layer(CorsLayer::permissive())
        .layer(TraceLayer::new_for_http());

    // 6. 绑定端口
    let addr = SocketAddr::from(([0, 0, 0, 0], config.port));
    tracing::info!("Aspectus v{} starting on {}", env!("CARGO_PKG_VERSION"), addr);

    let listener = tokio::net::TcpListener::bind(addr).await?;
    axum::serve(listener, app).await?;

    Ok(())
}
```

### 6.2 配置

```rust
// crates/aspectus-server/src/config.rs

#[derive(Debug, Clone)]
pub struct Config {
    pub port: u16,
    pub database_url: String,
    pub redis_url: String,
}

impl Config {
    pub fn from_env() -> anyhow::Result<Self> {
        Ok(Self {
            port: std::env::var("PORT")
                .unwrap_or_else(|_| "3001".into())
                .parse()?,
            database_url: std::env::var("DATABASE_URL")
                .context("DATABASE_URL must be set")?,
            redis_url: std::env::var("REDIS_URL")
                .unwrap_or_else(|_| "redis://localhost:6379".into()),
        })
    }
}
```

### 6.3 数据库连接池

```rust
// crates/aspectus-server/src/db/mod.rs

use sqlx::postgres::PgPoolOptions;
use sqlx::PgPool;

pub async fn init_pool(database_url: &str) -> anyhow::Result<PgPool> {
    let pool = PgPoolOptions::new()
        .max_connections(20)
        .min_connections(5)
        .connect(database_url)
        .await?;

    // 验证连接
    sqlx::query("SELECT 1").execute(&pool).await?;
    tracing::info!("PostgreSQL connection pool established");

    Ok(pool)
}
```

### 6.4 RFC 7807 错误格式

```rust
// crates/aspectus-server/src/error.rs

use axum::{
    http::StatusCode,
    response::{IntoResponse, Response},
    Json,
};
use serde::Serialize;

#[derive(Debug, Serialize)]
pub struct ProblemDetails {
    #[serde(rename = "type")]
    pub type_uri: String,

    pub title: String,

    pub status: u16,

    pub detail: String,

    #[serde(skip_serializing_if = "Option::is_none")]
    pub instance: Option<String>,

    #[serde(skip_serializing_if = "Option::is_none")]
    pub errors: Option<Vec<ValidationError>>,
}

#[derive(Debug, Serialize)]
pub struct ValidationError {
    pub field: String,
    pub message: String,
}

impl ProblemDetails {
    pub fn unauthorized(detail: impl Into<String>, instance: impl Into<String>) -> Self {
        Self {
            type_uri: "https://aspectus.dev/errors/unauthorized".into(),
            title: "Unauthorized".into(),
            status: 401,
            detail: detail.into(),
            instance: Some(instance.into()),
            errors: None,
        }
    }

    pub fn forbidden(detail: impl Into<String>) -> Self {
        Self {
            type_uri: "https://aspectus.dev/errors/forbidden".into(),
            title: "Forbidden".into(),
            status: 403,
            detail: detail.into(),
            instance: None,
            errors: None,
        }
    }

    pub fn not_found(detail: impl Into<String>) -> Self {
        Self {
            type_uri: "https://aspectus.dev/errors/not-found".into(),
            title: "Not Found".into(),
            status: 404,
            detail: detail.into(),
            instance: None,
            errors: None,
        }
    }

    pub fn validation_failed(
        detail: impl Into<String>,
        errors: Vec<ValidationError>,
    ) -> Self {
        Self {
            type_uri: "https://aspectus.dev/errors/validation-failed".into(),
            title: "Validation Failed".into(),
            status: 422,
            detail: detail.into(),
            instance: None,
            errors: Some(errors),
        }
    }

    pub fn internal_error(detail: impl Into<String>) -> Self {
        Self {
            type_uri: "https://aspectus.dev/errors/internal-error".into(),
            title: "Internal Server Error".into(),
            status: 500,
            detail: detail.into(),
            instance: None,
            errors: None,
        }
    }
}

impl IntoResponse for ProblemDetails {
    fn into_response(self) -> Response {
        let status = StatusCode::from_u16(self.status).unwrap_or(StatusCode::INTERNAL_SERVER_ERROR);
        let mut response = Json(self).into_response();
        *response.status_mut() = status;
        response.headers_mut().insert(
            axum::http::header::CONTENT_TYPE,
            "application/problem+json".parse().unwrap(),
        );
        response
    }
}
```

---

## 7. 认证 Stub (`aspectus-auth`)

```rust
// crates/aspectus-auth/src/lib.rs

use aspectus_core::{
    api_key::{ApiKey, CreatedApiKey},
    introspect::IntrospectResponse,
};

/// API Key 验证器 — v0.2.0 实现
pub struct ApiKeyVerifier;

impl ApiKeyVerifier {
    #[allow(unused)]
    pub fn new(/* db: PgPool, redis: RedisConnectionManager */) -> Self {
        Self
    }

    /// 验证 API Key 并返回自省响应
    /// v0.2.0 实现：sha256 → Redis → PostgreSQL
    pub async fn verify(&self, _token: &str) -> IntrospectResponse {
        unimplemented!("v0.2.0")
    }
}

/// Service Token 验证器 — v0.2.0 实现
pub struct ServiceTokenVerifier;

impl ServiceTokenVerifier {
    #[allow(unused)]
    pub fn new(/* db: PgPool, redis: RedisConnectionManager */) -> Self {
        Self
    }

    /// 验证 Service Token 并返回 Project 身份
    /// v0.2.0 实现：sha256 → Redis → PostgreSQL
    pub async fn verify(&self, _token: &str) -> Option<aspectus_core::project::Project> {
        unimplemented!("v0.2.0")
    }
}

/// API Key 创建器 — v0.2.0 实现
pub struct ApiKeyCreator;

impl ApiKeyCreator {
    #[allow(unused)]
    pub fn new(/* db: PgPool */) -> Self {
        Self
    }

    /// 生成 API Key，返回原文（仅一次），存储 sha256
    pub async fn create(
        &self,
        _tenant_id: &str,
        _service_account_id: &str,
        _project: aspectus_core::project::Project,
        _scopes: Vec<String>,
    ) -> Result<CreatedApiKey, aspectus_core::error::CoreError> {
        unimplemented!("v0.2.0")
    }
}
```

---

## 8. Client Stub (`aspectus-client`)

```rust
// crates/aspectus-client/src/lib.rs

use aspectus_core::introspect::IntrospectResponse;
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct IntrospectRequest {
    pub token: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub token_type_hint: Option<String>,
}

/// Aspectus HTTP client — v0.2.0 实现
pub struct AspectusClient {
    base_url: String,
    service_token: String,
    // client: reqwest::Client
}

impl AspectusClient {
    pub fn new(base_url: impl Into<String>, service_token: impl Into<String>) -> Self {
        Self {
            base_url: base_url.into(),
            service_token: service_token.into(),
        }
    }

    /// 调用 /introspect 端点
    /// v0.2.0 实现：POST {base_url}/introspect
    pub async fn introspect(
        &self,
        _token: &str,
    ) -> Result<IntrospectResponse, IntrospectError> {
        unimplemented!("v0.2.0")
    }
}

#[derive(Debug, thiserror::Error)]
pub enum IntrospectError {
    #[error("HTTP request failed: {0}")]
    Http(#[from] reqwest::Error),

    #[error("Service token rejected (401)")]
    Unauthorized,

    #[error("Unexpected response status: {0}")]
    UnexpectedStatus(u16),
}
```

---

## 9. Docker Compose

```yaml
# docker-compose.yml
services:
  postgres:
    image: postgres:17-alpine
    container_name: aspectus-postgres
    environment:
      POSTGRES_USER: aspectus
      POSTGRES_PASSWORD: aspectus_dev
      POSTGRES_DB: aspectus
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U aspectus"]
      interval: 3s
      timeout: 3s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: aspectus-redis
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 3s
      timeout: 3s
      retries: 5

volumes:
  pgdata:
```

---

## 10. 环境变量

```bash
# .env.example

# Server
PORT=3001

# PostgreSQL
DATABASE_URL=postgres://aspectus:aspectus_dev@localhost:5432/aspectus

# Redis
REDIS_URL=redis://localhost:6379

# Logging
RUST_LOG=aspectus_server=debug,tower_http=debug

# Service Tokens (v0.2.0+)
# Comma-separated: PROJECT=token
# SERVICE_TOKENS=pandaria=secret123,tavern=secret456
```

---

## 11. CI/CD

```yaml
# .github/workflows/ci.yml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-D warnings"

jobs:
  check:
    name: Check
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_USER: aspectus
          POSTGRES_PASSWORD: aspectus_test
          POSTGRES_DB: aspectus_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 3s
          --health-timeout 3s
          --health-retries 5
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 3s
          --health-timeout 3s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4

      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          components: rustfmt, clippy

      - uses: Swatinem/rust-cache@v2

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy --all-targets --all-features

      - name: Build
        run: cargo build --workspace

      - name: Install sqlx-cli
        run: cargo install sqlx-cli --no-default-features --features postgres,rustls

      - name: Run migrations
        run: sqlx migrate run
        env:
          DATABASE_URL: postgres://aspectus:aspectus_test@localhost:5432/aspectus_test

      - name: Test
        run: cargo test --workspace
        env:
          DATABASE_URL: postgres://aspectus:aspectus_test@localhost:5432/aspectus_test
          REDIS_URL: redis://localhost:6379

  security:
    name: Security Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions-rust-lang/setup-rust-toolchain@v1

      - name: Run cargo-deny
        uses: EmbarkStudios/cargo-deny-action@v1
        with:
          command: check bans licenses sources
```

---

## 12. justfile（开发命令）

```makefile
# justfile

# 默认命令
default:
    @just --list

# 启动开发环境
dev:
    docker compose up -d
    cargo run -p aspectus-server

# 运行 migration
migrate:
    sqlx migrate run

# 回滚 migration
migrate-rollback:
    sqlx migrate revert

# 运行全部测试
test:
    cargo test --workspace

# 运行测试（含输出）
test-verbose:
    cargo test --workspace -- --nocapture

# 代码检查
lint:
    cargo fmt --all -- --check
    cargo clippy --all-targets --all-features

# 自动格式化
fmt:
    cargo fmt --all

# 清理
clean:
    cargo clean
    docker compose down -v
```

---

## 13. .gitignore

```gitignore
# Rust
target/
**/*.rs.bk

# Environment
.env
!.env.example

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Docker
pgdata/

# Agent
.superpowers/
```

---

## 14. 验收标准

### 编译

```bash
cargo build --workspace
# → 0 errors, 0 warnings
```

### 测试

```bash
cargo test --workspace
# → 0 test cases (no logic yet), but test framework is operational
```

### 基础设施

```bash
docker compose up -d
# → postgres: healthy, redis: healthy

cargo run -p aspectus-server
# → "Aspectus v0.1.0 starting on 0.0.0.0:3001"

curl http://localhost:3001/health
# → 200 "ok"
```

### 数据库

```bash
sqlx migrate run
# → 2 migrations applied successfully

psql $DATABASE_URL -c "\dt"
# → 列出全部 10 张表 + 3 个枚举类型
```

### CI

```
GitHub Actions:
  ✅ cargo fmt --check
  ✅ cargo clippy
  ✅ cargo build --workspace
  ✅ cargo test --workspace
```

---

## 15. v0.1.0 不做的事

| ❌ | 说明 |
|----|------|
| 业务逻辑 | 不实现任何 CRUD、验证、创建 |
| HTTP endpoint（除 /health） | `/introspect`、管理 API 都在 v0.2.0 |
| DB 查询 | 连接池初始化即可，不执行业务查询 |
| API Key 生成 | `ApiKeyCreator` 是 stub |
| Token 验证 | `ApiKeyVerifier` / `ServiceTokenVerifier` 是 stub |
| Redis 连接 | pool 声明但不使用 |
| 配置热更新 | 只读环境变量 |
| 日志采样 | v0.2.0 |
| Metrics | v0.2.0 |

---

## 16. 参考资料

| ADR | 在本 spec 的应用 |
|-----|-----------------|
| [004](./adr/004-user-vs-service-account-role-type.md) | `identity_type` enum, `role_type` enum, `users`/`service_accounts` 表分离 |
| [005](./adr/005-role-global-definition.md) | `roles` 表不含 `tenant_id`（全局定义） |
| [006](./adr/006-scope-format.md) | `scopes.name` 格式为 `project:resource:action` |
| [008](./adr/008-single-layer-multi-tenancy.md) | 单层 `tenant_id`，无 organization 表 |
| [009](./adr/009-audit-log-structured-vs-jsonb.md) | `audit_logs` 结构化列 + `metadata JSONB` |
| [010](./adr/010-project-static-enum.md) | `project` enum 硬编码 6 个值 |
| [011](./adr/011-service-token-separate-auth.md) | `service_tokens` 表，每个 project 一个 |
| [012](./adr/012-technology-stack.md) | Rust/axum/PostgreSQL/Redis |
| [014](./adr/014-error-handling-rfc7807.md) | `ProblemDetails` struct |
| [015](./adr/015-id-format-short-id.md) | 所有 ID 使用 `varchar(21)`，KSUID 生成 |
